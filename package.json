{
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && concurrently \"npm run start:front\" \"npm run start:back\"",
    "start:front": "cd frontend && npm start",
    "start:back": "cd backend && python -m uvicorn app.main:app --reload"
  },
  "dependencies": {
    "@types/react": "^18.3.27",
    "@types/react-dom": "^18.3.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
